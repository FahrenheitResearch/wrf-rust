[package]
name = "wrf-sounding"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "WRF point and box sounding extraction"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
wrf-core = { path = "../wrf-core" }
