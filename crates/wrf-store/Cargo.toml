[package]
name = "wrf-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Local WRF run and file discovery primitives"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
